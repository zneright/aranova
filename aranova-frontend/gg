    setTimeout(() => {
      setLoading(false);

      if (isLogin) {
        if (
          form.email === "admin@aranova.com" &&
          form.password === "password123"
        ) {
          window.location.href = "/admin";
          return;
        } else if (
          form.email === "user@aranova.com" &&
          form.password === "password123"
        ) {
          window.location.href = "/user";
          return;
        } else {
          setErrors({
            email:
              "Try admin@aranova.com or user@aranova.com (pw: password123)",
          });
          return;
        }
      }